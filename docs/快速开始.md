# 快速开始（傻瓜式）

把下面三处里的占位符换成你自己的值即可。仓库里没有真实密钥。

| 占位符 | 含义 |
| --- | --- |
| `gateway.example.com` | 你的网关域名 |
| `<随机密钥>` | 自己生成的长随机串（≥32 字符） |
| `<Cursor API Key>` | Cursor 控制台里的 API Key |
| `C:\Workspaces\project1` | 允许 Runner 访问的本地目录 |

---

## 安全路径优先级（抗企业 TLS MITM）

1. **签名浏览器扩展**或 **本机 Secure Adapter**（内置离线 Ed25519 根）— 推荐
2. 纯网页 / PWA — **不能**承诺在“从一开始就被 MITM”环境下的首次安装安全

可信 CS 双跳 + 多设备历史规格见 [`trusted-cs-relay.md`](./trusted-cs-relay.md)；运维灰度见 [`cs-relay-ops.md`](./cs-relay-ops.md)。

## 第一步：准备 VPS（约 10 分钟）

1. 安装 Docker（含 Compose 插件）。
2. 克隆本仓库到例如 `/opt/cursor-gateway`。
3. **推荐**：启动傻瓜式部署页，鉴权后初始化（自动生成密钥，无需手工填一堆环境变量）：

```bash
cd /opt/cursor-gateway
./scripts/foolproof-deploy/start.sh
# 打开 http://127.0.0.1:19090/ ，粘贴 bootstrap token（或走 Cloudflare Access）
# 取消 Dry-run →「部署/初始化」→ 可选「同步并重启 compose」
```

详见 [`foolproof-deploy.md`](foolproof-deploy.md)。

4. **或手工**复制环境文件并编辑：

```bash
cd /opt/cursor-gateway
cp .env.example .env
nano .env
```

5. **手工路径最少必改这些行：**

```bash
PUBLIC_ORIGIN=https://gateway.example.com
JWT_SECRET=<随机密钥A>
RUNNER_SHARED_SECRET=<随机密钥B>
POSTGRES_PASSWORD=<数据库密码>
DATABASE_URL=postgres://cursor_gateway:<数据库密码>@postgres:5432/cursor_gateway
ALLOWED_EMAILS=you@example.com
E2EE_REQUIRED_FOR_WEB=false
```

生成随机串示例：

```bash
openssl rand -hex 32
```

6. 启动（向导已执行「同步并重启」则可跳过）：

```bash
cd /opt/cursor-gateway/infra
docker compose up -d --build
curl -fsS http://127.0.0.1:18080/healthz
```

看到健康检查成功即可。

7. 把现有 Nginx/Caddy/Cloudflare 反代指到本机 `127.0.0.1:18080`，并把 `infra/nginx-gateway.conf` / `infra/Caddyfile` 里的域名改成你的。  
   建议用 **Cloudflare Access** 保护整站（除 Runner 回调路径按你的策略配置）。若要用公网打开部署页，将 `/deploy/` 反代到向导的 `127.0.0.1:19090`（见 [`foolproof-deploy.md`](foolproof-deploy.md)）。

---

## 第二步：准备 Local Runner（约 5 分钟）

1. 安装 [Node.js 22+](https://nodejs.org/)。
2. 在同一仓库根目录打开 PowerShell：

```powershell
.\apps\windows-runner\scripts\start-runner.ps1
```

3. 首次会生成 `apps\windows-runner\.env`，用记事本打开，填：

```text
GATEWAY_URL=https://gateway.example.com
RUNNER_SHARED_SECRET=<与 VPS 的 RUNNER_SHARED_SECRET 完全相同>
RUNNER_WORKSPACES=C:\Workspaces\project1
RUNNER_E2EE_ENABLED=true
RUNNER_LEGACY_ENABLED=false
CURSOR_API_KEY=<Cursor API Key>
```

若站点前面有 Cloudflare Access，还需要 Service Token：

```text
CF_ACCESS_CLIENT_ID=<client id>
CF_ACCESS_CLIENT_SECRET=<client secret>
```

4. 再运行一次启动脚本。  
5. （推荐）管理员 PowerShell 安装开机自启：

```powershell
.\apps\windows-runner\scripts\install-runner-daemon.ps1
```

---

## 第三步：配对可信扩展

1. 登录 Gateway 网页，点击「下载扩展」获取预构建 `cursor-gateway-secure.zip`（或自行构建 `apps/browser-extension/dist`）。解压后在 `chrome://extensions` 开发者模式下加载已解压目录。
2. Windows 上运行 `npm run pair:runner -w @cursor-gateway/windows-runner`，把 bundle 导入扩展并核对 fingerprint。
3. 把扩展显示的 client bundle 运行 `npm run pair:client -w @cursor-gateway/windows-runner -- <bundle>` 导回 Runner。
4. 将扩展 origin 写入 VPS 的 `E2EE_EXTENSION_ORIGINS`（本仓库固定 ID：`chrome-extension://oicmfijjdbjkjhnljcjhnojpeiobhefe`），重启服务。不要无备份清空整个 `.env`。

完整步骤与安全边界见 [e2ee.md](e2ee.md)。

### Secure Web 设备验证顺序（推荐）

Secure Web PWA 的设备验证优先级（每种备选都保留）：

1. **Runner 设备码（RAMC）** —— 主方案，免扫码免邮箱。Runner 终端显示一次性高熵码 + 6 词 SAS，浏览器输入并核对。见 [`runner-manual-code-pairing.md`](./runner-manual-code-pairing.md)。开启：服务端 `RUNNER_CODE_PAIRING_ENABLED=true`，Runner `RUNNER_CODE_ENABLED=true`。
2. 已授权设备批准
3. Passkey
4. 恢复码
5. 邮箱 magic-link / QR

手机首装用 **信任根 SAS**（独立渠道比对）与可选的**桌面 localhost verifier**（[`secure-web-verifier.md`](./secure-web-verifier.md)）锚定信任；RAMC 本身不证明首载 JS，需可信 PWA bootstrap 或 localhost verifier。

---

## 第四步：验证

1. 浏览器打开 `https://gateway.example.com`，用 Cloudflare Access 登录。
2. 打开签名扩展并选择已固定 fingerprint 的 Runner。
3. 选一个工作区，发一句只读问题，状态应变为 `finished`。
4. 验证后设置 `E2EE_REQUIRED_FOR_WEB=true`，让普通网页拒绝新明文聊天。

出问题先看：

- VPS：`docker compose -f infra/docker-compose.yml logs -f app`
- Windows：`apps\windows-runner\logs\` 下的日志与 `runner-health.json`

---

## 配置对照（避免填错）

| 配置项 | 填在哪里 | 两边要一致？ |
| --- | --- | --- |
| `PUBLIC_ORIGIN` / `GATEWAY_URL` | VPS `.env` / Windows `.env` | 域名应对应同一站点 |
| `RUNNER_SHARED_SECRET` | 两边都要 | **必须完全一致** |
| `JWT_SECRET` | 仅 VPS | — |
| `CURSOR_API_KEY` | 仅 Windows | — |
| `RUNNER_WORKSPACES` | 仅 Windows | — |
| `ALLOWED_EMAILS` | 仅 VPS | — |
| `TELEGRAM_*` | 仅 VPS（可选） | — |

---

## 不要做的事

- 不要把 `.env`、API Key、Cookie、Service Token 提交到 Git
- 不要把 Runner 工作区设成整个 `C:\Users`
- 不要对公网裸奔 18080 端口（应只监听本机并由反代/Access 保护）
