# Cursor Gateway runner configuration.
# scripts/setup-runner.sh creates a copy of this file as ".env" on first run.
# Fill in the values below, then run the setup script again.

# Public URL of your gateway (the server/VPS deployment).
GATEWAY_URL=https://gateway.example.com

# Shared secret used to authenticate the runner with the server.
# Must be identical to RUNNER_SHARED_SECRET on the server, 32+ characters.
RUNNER_SHARED_SECRET=replace-with-the-same-secret-used-by-the-server

# Directories the agent may read/write, separated by ";".
# Use absolute paths that exist on THIS machine.
# Linux/WSL example: /home/you/projects
# (Windows-style paths like D:\work are auto-mapped to /mnt/d/work on WSL.)
RUNNER_WORKSPACES=/home/you/projects

# Your Cursor API key (starts with crsr_ or cursor_). Create one at https://cursor.com.
CURSOR_API_KEY=

# Label shown for this runner in the dashboard.
RUNNER_ID=main-runner

# Default model to use when a request does not specify one.
DEFAULT_MODEL=auto

# Advanced settings (safe defaults).
RUNNER_POLL_INTERVAL_MS=2000
RUNNER_MAX_CONCURRENT_JOBS=3

# Only required if the gateway is protected by Cloudflare Access.
# Leave blank otherwise.
CF_ACCESS_CLIENT_ID=
CF_ACCESS_CLIENT_SECRET=

# End-to-end encryption (see docs/e2ee.md). At least one of the two toggles
# below must be enabled. Defaults: E2EE on, legacy plaintext off.
RUNNER_E2EE_ENABLED=true
RUNNER_LEGACY_ENABLED=false
# DPAPI-protected key/pairing/replay state file (Windows default shown).
# Defaults to %USERPROFILE%\.cursor-gateway\runner-e2ee-state.dat
RUNNER_E2EE_STATE_FILE=
# Never enable outside a disposable non-Windows development test.
RUNNER_E2EE_ALLOW_INSECURE_DEV_STORAGE=false
# Linux/WSL at-rest protection (replaces DPAPI): passphrase-or-secret master key.
# Prefer a tmpfs path (e.g. /dev/shm/cursor-gateway/runner-e2ee-master.key) so the
# usable master key is not stored on the same persistent disk as the state file.
# Persist across reboot by sealing that key with seal-master-key.sh (passphrase
# KDF); after reboot run unseal-master-key.sh / e2ee-up.sh once.
# RUNNER_E2EE_MASTER_KEY=
# RUNNER_E2EE_MASTER_KEY_FILE=/dev/shm/cursor-gateway/runner-e2ee-master.key

# Secure-web magic-link pairing (see docs/secure-web-e2ee.md and docs/pairing-mail.md)
# SECURE_CLIENT_ORIGIN=https://secure.example.com
# PAIRING_TTL_SECONDS=900
# PAIRING_MAIL_MODE=log
# PAIRING_MAIL_TO=you@example.com
# PAIRING_MAIL_LOG_FILE=
# PAIRING_MAIL_FROM=no-reply@piallera.com
# PAIRING_MAIL_FROM_NAME=Piallera Secure
# PAIRING_MAIL_ALSO_LOG=false
# PAIRING_ALLOWED_EMAILS=you@example.com
# --- Real delivery (pick ONE of smtp or api; see docs/pairing-mail.md) ---
# PAIRING_MAIL_MODE=api
# MAIL_API_PROVIDER=resend
# MAIL_API_KEY=re_xxxxxxxx
# PAIRING_MAIL_MODE=smtp
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=resend
# SMTP_PASS=re_xxxxxxxx
# SMTP_URL=smtps://resend:re_xxxxxxxx@smtp.resend.com:465
# MAILGUN_BASE_URL=https://api.mailgun.net/v3/mg.example.com
# CF_ACCESS_TEAM_DOMAIN=https://yourteam.cloudflareaccess.com
# CF_ACCESS_AUD=
# CS web return origins for one-time device auth grants (see docs/cs-secure-redirect-e2ee.md)
# WEB_E2EE_RETURN_ORIGINS=https://cs.joelzt.org
# E2EE_CS_AUTH_GRANT_TTL_SECONDS=120
